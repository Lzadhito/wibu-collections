export function generateTxtTitle(animeName: string, collectionName: string) {
  return `Remove ${animeName} from ${collectionName}`;
}

export function generateTxtDesc(animeName: string, collectionName: string) {
  return `You're going to remove ${animeName} from ${collectionName}. Are you sure?`;
}
