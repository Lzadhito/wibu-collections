export function generateTxtConfirmationTitle(collectionName: string) {
  return `Remove ${collectionName}?`;
}
export function generateTxtConfirmationDesc(collectionName: string) {
  return `You are going to remove ${collectionName} from your collection. All anime inside the collection will be removed from your collection`;
}
